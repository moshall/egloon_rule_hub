# Manual TXT Sources

This directory is reserved for manually maintained TXT source files when no suitable upstream rule source exists.

Planned convention:

- one platform or service per file
- filename pattern: `<Platform>.txt`
- these TXT files will become source inputs for future automated conversion into client-specific rule artifacts

The concrete TXT line format is intentionally not fixed yet. It will be defined in a later implementation step.
